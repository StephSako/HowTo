{% macro show(user) %}
    <div class="chip blue lighten-1 white-text">
        <img src="{{ vich_uploader_asset(user, 'imageFile') }}" alt="">
        <span>{{ user.firstname|capitalize}} {{ user.lastname|upper }}</span>
    </div>
{% endmacro %}